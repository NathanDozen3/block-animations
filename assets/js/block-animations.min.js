window.onload = function () {
	function i( t ) {
		t.forEach( ( t ) => {
			var o = t.isIntersecting;
			let n = t.target;
			t = JSON.parse( t.target.dataset.blockAnimationsConfig ).delay ?? 0;
			! 0 === o
				? setTimeout( function () {
						n.classList.add( a );
				  }, t )
				: setTimeout( function () {
						n.classList.remove( a );
				  }, t );
		} );
	}
	let a = 'block-animate',
		e = { root: null, rootMargin: '0px', threshold: [ 0.5 ] };
	document
		.querySelectorAll( '[data-block-animations]' )
		.forEach( function ( t ) {
			var o,
				n = JSON.parse( t.dataset.blockAnimationsConfig );
			for ( o in e ) void 0 === n[ o ] && ( n[ o ] = e[ o ] );
			for ( o in n ) {
				var a = 'data-block-animations';
				'animation' !== o &&
					( ( a += '-' + o ), t.setAttribute( a, n[ o ] ) ),
					'duration' === o &&
						t.style.setProperty(
							'--blockAnimations-transition-' + o,
							n[ o ] + 'ms'
						);
			}
			new IntersectionObserver( i, n ).observe( t );
		} );
};
